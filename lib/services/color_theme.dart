import 'package:flutter/material.dart';

class Clr {
  static const white = Color(0xFFfffffff);
  static const black = Color(0xFF000000);
  static const noclr = Colors.transparent;
  static const orang = Colors.orange;
}
